import { TextField, TextFieldProps } from "@mui/material"
import { useField } from "@unform/core"

export const VTextFild = ({ name, ...props }) => {
    const {} = useField()

    return(
        <TextField />
    )
}